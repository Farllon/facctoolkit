name: Pack FaccToolkit.Persistence.MongoDb.Abstractions

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.MongoDb.Abstractions/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.MongoDb.Abstractions
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}