name: Pack FaccToolkit.Persistence.MongoDb.Rich

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.MongoDb.Rich/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.MongoDb.Abstractions", "Pack FaccToolkit.Domain.Rich"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.MongoDb.Rich
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}