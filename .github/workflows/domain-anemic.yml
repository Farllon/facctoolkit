name: Pack FaccToolkit.Domain.Anemic

on:
  push:
    branches: [master]
    paths:
      - 'libs/domain/FaccToolkit.Domain.Anemic/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Domain.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/domain/FaccToolkit.Domain.Anemic
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}