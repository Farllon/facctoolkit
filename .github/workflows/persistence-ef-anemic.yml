name: Pack FaccToolkit.Persistence.EntityFramework.AnemicDomain

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.EntityFramework.AnemicDomain/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.EntityFramework.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.EntityFramework.AnemicDomain
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}