name: Pack FaccToolkit.Persistence.EntityFramework.Anemic

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.EntityFramework.Anemic/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.EntityFramework.Abstractions", "Pack FaccToolkit.Domain.Anemic"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.EntityFramework.Anemic
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}