name: Pack FaccToolkit.Persistence.EntityFramework.RichDomain

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.EntityFramework.RichDomain/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.EntityFramework.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.EntityFramework.RichDomain
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}