name: Pack FaccToolkit.Persistence.EntityFramework.Abstractions

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.EntityFramework.Abstractions/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.EntityFramework.Abstractions
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}