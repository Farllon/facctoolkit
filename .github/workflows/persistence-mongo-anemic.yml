name: Pack FaccToolkit.Persistence.MongoDb.AnemicDomain

on:
  push:
    branches: [master]
    paths:
      - 'libs/persistence/FaccToolkit.Persistence.MongoDb.AnemicDomain/**'
  workflow_run:
    workflows: ["Pack FaccToolkit.Persistence.MongoDb.Abstractions"]
    types:
      - completed

jobs:
  build:
    uses: ./.github/workflows/build-nuget.yml
    with:
      project: libs/persistence/FaccToolkit.Persistence.MongoDb.AnemicDomain
    secrets:
      NUGET_PACKAGE_TOKEN: ${{ secrets.NUGET_PACKAGE_TOKEN }}